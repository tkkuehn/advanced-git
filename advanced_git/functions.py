"""Some useful(?) functions in a module."""


def add_one(num: float) -> float:
    """Add one to a given number."""
    return num + 1


def add_two(num: float) -> float:
    """Add two to a given number."""
    return num + 2
